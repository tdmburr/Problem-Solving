// Start with an array of strings with a mix of uppercase and lowercase letters. Print every word in all lowercase letters.

// Create an array with a mix of upper and lower case letters then print every word(string) in lowercase letters.
//print all words in lowercase
//use a console command to print everything as a lowercase value in the array

var words = ["CrAzY", "LeTteRS", "TIME"]

for (var i = 0; i < words.length; i++) { 
    console.log(words[i].toLowerCase())
}
